{
  "name": "visualearn",
  "version": "1.0.0",
  "description": "AI-Powered Interactive 3D Concept Visualizer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "test": "npm run test -w frontend && npm run test -w backend",
    "test:coverage": "npm run test:coverage -w frontend && npm run test:coverage -w backend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "lint:fix": "npm run lint:fix -w frontend && npm run lint:fix -w backend",
    "format": "npm run format -w frontend && npm run format -w backend",
    "type-check": "npm run type-check -w frontend && npm run type-check -w backend",
    "clean": "rm -rf frontend/dist backend/dist frontend/node_modules backend/node_modules"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
